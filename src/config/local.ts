export const Config = {
  // This should be updated and pointed to dev's local WP environment during development
  // OR if once site is deployed with real data URL should be URL of website
  fetchBase: 'http://localhost/wp_food_trust'
};
